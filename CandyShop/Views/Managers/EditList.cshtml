@model IEnumerable<CandyShop.Models.WorkHoursTracker>

@{
    ViewData["Title"] = "EditList";
}

<h1>Work History</h1>

<h4>Select times to edit</h4>
<table class="table">
    <thead>
        <tr>
            <th>
                Employee
            </th>
            <th>
                Clocked In
            </th>
            <th>
                Break Started
            </th>
            <th>
                Break Ended
            </th>
            <th>
                Clocked Out
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.employee.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.clockIn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.breakStart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.breakEnd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.clockOut)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.trackerId">EDIT</a>
            </td>
        </tr>
        }
    </tbody>
</table>

<div>
    <a asp-action="EmployeeListView">Back to Employee List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
