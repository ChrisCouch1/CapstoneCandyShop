@model IEnumerable<CandyShop.Models.Product>

@{
    ViewData["Title"] = "Index";
}

<h1>Welcome</h1>

<p>
    <span>@DateTime.Now</span>
    <span><button type="submit" asp-action="TimePunch" asp-action-id="Model.userId">Clock In/Out?</button></span>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Category
            </th>
            <th>
                Product Name
            </th>
            <th>
                Quantity
            </th>
            <th>
                Price
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => product.category)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => product.productName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => product.QTY)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => product.price)
                </td>
                <td>
                    <a asp-action="ProductDetails" asp-route-id="@product.productId">Product Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
